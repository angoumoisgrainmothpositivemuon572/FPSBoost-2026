const core = {
    id: 3973,
    tag: "lWDTJbN5MqB69",
};

const wpyhj = (arr) => arr.reduce((a, b) => a + b * 2, 0);

const values = Array.from({ length: 9 }, (_, i) => i);
console.log(wpyhj(values), core.tag);
